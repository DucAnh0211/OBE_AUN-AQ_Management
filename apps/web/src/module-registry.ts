/**
 * Web composition registry placeholder.
 * Concrete module imports are added after the frontend stack is initialized.
 */
export const webModuleIds = [
  "curriculum",
  "accreditation",
  "reporting",
] as const;
